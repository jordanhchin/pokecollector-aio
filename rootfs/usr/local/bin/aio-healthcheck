#!/bin/sh
set -eu
pg_isready -q -h 127.0.0.1 -U "${POSTGRES_USER:-pokemon}" -d "${POSTGRES_DB:-pokemon_tcg}"
curl -fsS --max-time 5 http://127.0.0.1:8000/api/health >/dev/null
curl -fsS --max-time 5 "http://127.0.0.1:${WEB_PORT:-3000}/" >/dev/null

